---
applications:
- name: govuk-prototype-kit
  command: rm .env_vars && node start.js
  env:
    USE_AUTH: false
    PROMO_MODE: true
  memory: 1G
  instances: 1
  buildpacks:
    - nodejs_buildpack
