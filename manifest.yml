---
applications:
- name: govuk-prototype-kit
  command: >
    rm Procfile &&
    echo {\"collectUsageData\":false} > usage-data-config.json &&
    rm .env_vars &&
    node start.js
  env:
    USE_AUTH: false
    PROMO_MODE: true
  memory: 1G
  instances: 1
  buildpacks:
    - nodejs_buildpack
